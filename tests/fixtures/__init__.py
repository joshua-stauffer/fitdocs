"""Deterministic synthetic ``.fit`` fixture builders (test support only).

This subpackage never imports ``src/fitdocs``; it produces ``.fit`` bytes and
decoded message dicts for the suite to feed through the code under test.
"""
