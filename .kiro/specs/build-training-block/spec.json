{
  "feature_name": "build-training-block",
  "created_at": "2026-09-16T06:29:18Z",
  "updated_at": "2026-09-16T08:05:00Z",
  "language": "en",
  "phase": "tasks-generated",
  "approvals": {
    "requirements": {
      "generated": true,
      "approved": true
    },
    "design": {
      "generated": true,
      "approved": true
    },
    "tasks": {
      "generated": true,
      "approved": true
    }
  },
  "ready_for_implementation": true,
  "phase_note": "Generated 2026-09-16 by the Phase 7 spec batch (wave 3) in auto-approve mode. 7 requirements / 48 acceptance criteria; 9 design components; 3 major tasks / 8 executable tasks (3.1, 3.2, 3.3 parallel after major 2; 3.4 last). Step 3.5 task-graph sanity review ran as an independent reviewer subagent on model opus, twice. Round 1: RETURN_TO_DESIGN on one narrow, real contradiction (requirement 3.8 required the skill to name the sync/regen chaining, requirement 4.2 forbade 'regen' anywhere outside the never-does section) plus 6 should-fix and 4 notes; because this run owns the whole pipeline, the requirement was repaired in place (4.2 now scopes the rule to fences plus a same-line-as-sync clause) and every other finding applied to design.md and the draft tasks -- command-binding scan scoped to code spans and fence lines, the stem locations corrected against plan-resolution's placement grammar, per-task mypy entries, the distribution amendment widened to cover its version-literal scan and task 1.4's singular bullet, the negative e2e stage fully specified as the supersession exercise, a no-writes mutation, the AST pin names re-anchored to post-plan-resolution cli.py, today pinned through the fake-date contextmanager, files bound at module level. Round 2: NEEDS_FIXES with no blocking finding (4 should-fix, 5 notes: a stale invariant line, a self-contradicting grep observable, an unanchored report-token list, a circular 4.1->4.2 dependency in the amended distribution plan, and five wording precisions); all nine applied and verified by exact-string checks against the files on disk rather than independently re-reviewed, because the skill bounds the loop at one re-run -- stated here as the downgrade it is. Lands the roadmap Phase 7 Existing Spec Update for distribution as Amendment 1 (task 3.3), with the 'major 4 ships first' alternative as a recorded precondition in task 1.1 (LANDED/WIDENED) that task 3.3 reads; distribution's 4.1 is ticked as landed and its residual folded into 4.2 because this spec's both-ways registry pin makes a name-before-directory split unmergeable. Upstream seams consumed verbatim and stated in design.md 'Cross-spec obligations': BlockStatus, parse_block/Block and the four change kinds, RowState, Confidence, the placement grammar's cell/sentence shapes, --out as plan's only option, latest-override-wins, missing stem renders and exits 1, plans/ never created. Nothing is asked of either upstream spec; the one unbound surface (the reconcile report's line prefixes, prose in cli._report_reconcile) is stated as a limitation and pinned behaviourally by the e2e walk. Packaging facts verified empirically this session: a wheel built from a tree carrying probe files shipped skills/build-training-block/{SKILL.md,example-block.toml} and silently dropped both data/ probes (.gitignore). Deviations from the brief: cli.py:5 'Four feature commands' is stale (seven registered) and is corrected by task 1.2 rather than queued; the methodology-table importlib.resources precedent distribution's design cites no longer exists (purged), so this is the first files() site. INCIDENT, recorded in .kiro/queue/closed/2026-09-16-main-repo-git-database-destroyed-partially-restored.md: a packaging probe meant for the scratchpad ran in /Users/josh/code/fitdocs_oss after a failed cd and destroyed the main repository's .git (local main 3664a0d, the batch branch, the worktree link and .git/agent-log); working trees are intact; origin was re-fetched and HEAD/main restored to the remote tip ab6309e with the two lost commits' content visible as a working-tree diff; the auto-mode classifier then refused further .git writes, so the branch/worktree re-link, the re-created commits and the agent-log rebuild are the controller's, per the queue item. The Step 3.5 reviewers were instructed not to run git; this spec's files were written by the file tools and are on disk in the worktree. RESTORED by the controller the same day, additively: main re-created as 4981bb2/5dff756 (twins of 5def7a7/3664a0d, identical trees), the worktree relinked, waves 1-3 re-committed (b381f0d, bdfeb8a, then this spec), and .git/agent-log rebuilt byte-exact from the controller's session-start read; the queue item is closed as done and the hardening it owes is 2026-09-16-change-guard-deny-rm-rf-git."
}
