{
  "feature_name": "wiki-contract",
  "created_at": "2026-07-21T07:56:54Z",
  "updated_at": "2026-09-11T00:00:00Z",
  "language": "en",
  "phase": "tasks-generated",
  "approvals": {
    "requirements": {
      "generated": true,
      "approved": true
    },
    "design": {
      "generated": true,
      "approved": true
    },
    "tasks": {
      "generated": true,
      "approved": true
    }
  },
  "ready_for_implementation": true,
  "amendments": [
    {
      "date": "2026-07-22",
      "requirement": "3.2 / new 3.2a",
      "reason": "Req 3.2 read literally mandated a false claim for fit-archive/ (re-derivability). Task 4.1 was rejected three times on emitted-text falsehoods rooted in this. Human-ratified carve-out: 3.2's re-derivability and user-owned-region elements apply only to directories holding generated documents; 3.3 governs the archive.",
      "also": "Task 4.1 emitted prose reduced to the mandated 6+1 claims; long-form editing guidance deferred to task 7.1's published contract."
    },
    {
      "date": "2026-07-22",
      "requirement": "5.5 / 5.8 (LoadEngine version gate) and structural (docio.py)",
      "reason": "Task 7.2 found sync and the load engine each carrying its own local frontmatter read, functionally identical to each other and BOTH lacking the symlink refusal that audit._read_document already had -- so a symlinked workouts/*.md was refused by `check` and followed by every writer, letting a write escape the data root (Req 7.5, 7.6). The two were collapsed into src/fitdocs/docio.py, one enforcement point; deleting its refusal now fails five tests where it previously failed none. Separately, the document-format version gate was duplicated into load/engine.py because the training-load pass is itself a writer, so Req 5.5's 'left unchanged' guarantee held for the engine functions but not for the CLI commands that run that pass afterwards.",
      "also": "design.md's File Structure Plan (New Files) now lists docio.py; the LoadEngine component section now states the version-gate duplication and its requirement coverage; a fourth cross-spec integration obligation records that a writing entry point must read through docio.read_frontmatter and run the once-per-run symlink scan beside refresh_declarations; tests/test_public_api.py now pins docio's exported surface alongside contract, declaration, and audit."
    },
    {
      "date": "2026-09-11",
      "requirement": "6.5, 6.6, 6.7, 6.8 (new)",
      "reason": "effort-tags spec task 4.3: effort-tags adds a second class of user-owned frontmatter key (the effort tag's four keys, contract.USER_KEYS) alongside the user-owned regions this spec already names. This spec's frontmatter-key-ownership contract is extended to cover the class generically -- publication as disjoint from the managed set, byte-for-byte carry-forward with rebuild placement, and exclusion from the unmanaged-key warning (6.3) and finding (8.4) -- while the vocabulary, validation, and typed reader of the effort tag itself remain effort-tags' own.",
      "also": "requirements.md gains Amendment 1 (2026-09-11) and Requirement 6 criteria 6.5-6.8; design.md's DocumentContract component gains a matching amendment note (USER_KEYS, user_owned_lines, the unmanaged_keys postcondition change) and 'This Spec Owns' gains a bullet for the user-owned key class. Nothing renumbered."
    },
    {
      "date": "2026-09-11",
      "requirement": "2.11, 3.9 (new) -- implemented by the load-history spec, not by this one",
      "reason": "load-history spec task 5.5, the second half of the roadmap's Phase 6 Existing Spec Update for wiki-contract (the first half, 6.5-6.8 above, was landed by effort-tags). load-history adds a new owned data-root location (history/, history/assets/) holding one training-load history page -- a second document type distinct from the workout document this spec defines. This spec's published-ownership-contract guarantee (2.11) is extended to name the location as fitdocs-owned and to state that a second document type exists there, declared and versioned by load-history rather than by this contract; its in-tree-declaration guarantee (3.9) is extended to state what the location's own declaration says about that document -- one generated page and its chart image, rewritten in full, no user-owned region, re-derivable from the workout documents alone -- without restating the document's type or schema, which 2.11 locates. The location's write path, its declaration text, and the page's own schema remain load-history's own.",
      "also": "requirements.md gains Amendment 2 (2026-09-11), Requirement 2 criterion 2.11 and Requirement 3 criterion 3.9; design.md's DocumentContract component gains a matching amendment note (the second type, its declaration in fitdocs.history.page, and why it is not published from this leaf). CONTRACT_VERSION advances \"2\" to \"3\" in code by load-history task 1.2, not restated here. Nothing renumbered."
    }
  ]
}
