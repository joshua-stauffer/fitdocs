{
  "feature_name": "training-blocks",
  "created_at": "2026-09-15T21:24:39Z",
  "updated_at": "2026-09-16T08:06:38Z",
  "language": "en",
  "phase": "tasks-generated",
  "approvals": {
    "requirements": {
      "generated": true,
      "approved": true
    },
    "design": {
      "generated": true,
      "approved": true
    },
    "tasks": {
      "generated": true,
      "approved": true
    }
  },
  "ready_for_implementation": true,
  "phase_note": "Generated 2026-09-15/16 by the Phase 7 spec batch (wave 1) in auto-approve mode. 8 requirements / 73 acceptance criteria; 15 design components; 4 major tasks / 15 executable tasks (3.2 and 3.3 parallel after 3.1). Step 3.5 task-graph sanity review ran as an independent reviewer subagent on model opus, twice: round 1 NEEDS_FIXES (2 blocking, 8 should-fix, 8 notes) -- every finding applied to tasks.md and mirrored into design.md; round 2 PASS with 4 should-fix and 2 notes (prose contradictions the round-1 repairs left behind), all six applied and verified by exact-string checks against the files on disk rather than independently re-reviewed, because the skill bounds the loop at one re-run -- stated here as the downgrade it is. Two of round 1's blocking findings were verified against the tree before repair: tests/history/test_engine.py:934 hand-enumerates the declared directories (task 1.2 now moves that one constant; nothing under src/fitdocs/history/ changes), and the two parallel renderer tasks needed three shared helpers in plans/page.py (task 3.1 now provides check_resolution, is_fence_line, sport_phrase). Lands the roadmap Phase 7 Existing Spec Update for wiki-contract as Amendment 3 (task 4.5). Seams for wave 2 are stated verbatim under design.md 'Cross-spec obligations (training-blocks <-> plan-resolution)': the Resolution placement type and its unresolved default, the blocks/ location with the 'plan' entry point, run_plan's resolve= hook, and Block as the input. Accepted trade-offs: design.md ~1750 lines, over the template's 1000-line advisory, for the same reason load-history's was (layout+ownership, a source grammar, a model, two renderers, a pass, CLI and four guard surfaces in one spec, per the roadmap); tasks 3.2 and 4.2 exceed the 1-3 hour guideline but are single-boundary (reviewer N6) -- 4.2 splits cleanly into gating/discovery/report and the per-block pipeline if the controller wants smaller units. Deviations from the brief: the mypy files list needs no src entry (\"src\" already covers the package) so the roadmap's 'mypy files list moves' item reduces to the four typed test modules; the brief's line citations drifted cosmetically (test_confinement.py EntryPoint :568-588, ownership-contract.md shared files :357-386, contract.py wikilink note :517-519) and were re-anchored. Queue: this spec added 2026-09-15-atomic-write-helper-copied-per-engine; task 1.3 absorbs 2026-09-10-ownership-contract-fitdocs-toml-tables-stale and part of 2026-09-12-ownership-contract-prose-stale-and-unpinned (which names a phantom tests/test_ownership_contract_doc.py -- not edited here; the controller owns queue bookkeeping). Cross-spec review round 1 (2026-09-16): issues 1(TB side), 2, 4, 5, 6 applied -- sport_phrase emits its indoor word from page.INDOOR_WORD so plan-resolution can rebind it to contract.INDOOR_KEY with one edit (task 3.1, design PageVocabulary and obligations item 6); criteria 8.4 and 8.8 scoped to the default resolution and to this spec's own chaining so plan-resolution's resolver and chaining contradict neither, with design obligations item 5 naming the two pins (4.3 AST, 4.6 two-dates) that spec re-anchors; tasks.md 'Cross-spec shared files' now lists every file plan-resolution touches, split into append-only and re-anchored-by-exception, and the prerequisite line says this plan is upstream of both downstream specs; task 4.5 ticks the roadmap's wiki-contract checkbox; a TargetOp on a mesocycle with no stated target is valid with before = MesocycleTarget(number, None, None) rendered (unset) (design PlanModel and grammar table, requirement 3.4, task 2.2)."
}
